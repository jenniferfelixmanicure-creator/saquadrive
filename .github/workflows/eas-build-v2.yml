name: EAS Build – zeroriscov2 (Preview APK)

on:
  push:
    branches: [main]
    paths:
      - 'artifacts/zeroriscov2/**'
  workflow_dispatch:

concurrency:
  group: eas-build-zeroriscov2
  cancel-in-progress: true

jobs:
  build:
    name: EAS Build Android APK v2
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repositório
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Instalar pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Instalar dependências
        run: pnpm install --no-frozen-lockfile

      - name: Instalar EAS CLI
        run: npm install -g eas-cli

      - name: EAS Build (Preview APK)
        working-directory: artifacts/zeroriscov2
        run: eas build --profile preview --platform android --non-interactive --no-wait
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
